#### 核心问题定位

&#x20;

SANSA 的 support→query 匹配链：

```
Support backbone feat + part mask
    → _encode_new_memory（冻结）→ maskmem_features [B,64,32,32]
    → 存入 memory_bank，同时存 obj_ptr [B,256]

Query 解码时：
    _prepare_memory_conditioned_features（冻结）
        Q: query feat[-1]   KV: {maskmem_features, obj_ptr}
    → pix_feat_with_mem → SAM decoder → part mask
```

**瓶颈**：`obj_ptr` 是从 SAM mask decoder 的 output token 导出的 256-dim 全局描述子，编码的是"物体级 identity"；对部件分割而言，它应当携带"这是哪个部件"的细粒度语义，但当前做不到。

***

#### 方向一：Part Prototype → obj\_ptr 增强

**动机（对应 survey 中 identity mismatch 问题）**

SAM2 的 obj\_ptr 被设计为 video tracking 中的物体指针，SANSA 直接复用。对部件分割，同一物体的不同部件（头 vs 翅膀）在 obj\_ptr 空间几乎不可区分。需要将部件原型信息注入 obj\_ptr。

**插入位置**：`sansa.py` → `_compute_memory_bank_dict`，在 SAM2 输出 `obj_ptr` 之后、写入 `memory_bank` 之前。

**伪代码**：

python

```
# _compute_memory_bank_dict 内，encode_new_memory 之后

# 1. 从 support backbone 特征提取部件原型（仅支持帧）
feat_32 = rearrange(current_vision_feats[-1], '(h w) b c -> b c h w')
mask_32 = interpolate(decoder_out.high_res_masks > 0, size=(32,32))
part_proto = masked_avg_pool(feat_32, mask_32)          # [B, 256]

# 2. 残差注入 obj_ptr（可训练: Linear 256→256）
enhanced_obj_ptr = decoder_out.obj_ptr + self.part_proto_proj(part_proto)

memory_bank[idx] = {
    "maskmem_features": mem_feats,
    "maskmem_pos_enc": mem_pos,
    "pred_masks": decoder_out.low_res_masks,
    "obj_ptr": enhanced_obj_ptr,   # 替换原 obj_ptr
}
```

**为何有效**：`obj_ptr` 在 `_prepare_memory_conditioned_features` 中作为额外 token 参与 query 的 cross-attention。注入部件原型后，cross-attention 会自动将 query 特征向与原型相似的区域对齐。整个 SAM2 不需任何修改。

**新增参数**：`Linear(256→256)`，约 65K。

***

#### 方向二：原型相关图作为 Dense Prompt

**动机（对应 survey 中 HSNet/P2SAM 的显式相关性计算）**

即使 obj\_ptr 得到了增强，memory attention 仍是隐式的特征匹配，对小型部件（眼睛、把手）定位能力弱。直接计算 support 部件原型与 query 特征的余弦相似度图，将其作为显式空间先验注入 SAM decoder。

SAM 的 prompt encoder 原生支持 mask 作为 dense prompt（`_embed_masks`），这恰好是合适的注入接口。

**插入位置**：`sansa.py` → `_compute_decoder_out_w_mem`，在 `_prepare_memory_conditioned_features` 之后、`_forward_sam_heads` 之前。

**伪代码**：

python

```
# _compute_decoder_out_w_mem 内

pix_feat_with_mem = sam._prepare_memory_conditioned_features(...)  # 不变

# 1. 取出 memory_bank 中存储的 part_proto（由方向一计算）
part_proto = memory_bank[0]["part_proto"]   # [B, 256]

# 2. 计算 query 特征与 part_proto 的余弦相似度图
query_feat = rearrange(current_vision_feats[-1], '(h w) b c -> b c h w')  # [B,256,32,32]
corr_map = cosine_sim(query_feat, part_proto)   # [B, 1, 32, 32]

# 3. 上采样至 SAM mask input size（256×256），作为 dense prompt
corr_prompt = interpolate(corr_map, size=(256, 256))   # SAM prompt encoder 期望的尺寸

decoder_out = sam._forward_sam_heads(
    backbone_features=pix_feat_with_mem,
    mask_inputs=corr_prompt,           # 注入相关图
    high_res_features=high_res_features,
)
```

**为何有效**：SAM 的 mask prompt 经 `PromptEncoder._embed_masks`（4层卷积下采样 + 投影）编码为 dense embedding，与 sparse\_embeddings 一起进入 TwoWayTransformer。相关图高值区域自然指向部件位置，提供 memory attention 无法给出的精确空间定位。

**新增参数**：若方向一已存储 `part_proto`，此方向零额外参数；若独立实现则加 `Linear(256→256)` 用于原型投影。

***

### 两方向关系

```
Support 帧处理：
    backbone feat + part mask
        → masked_avg_pool → part_proto（可训练投影）
        → 残差叠加到 obj_ptr  [方向一]
        → 存入 memory_bank

Query 帧解码：
    memory_attention(query_feat, {maskmem + enhanced_obj_ptr})
        → pix_feat_with_mem（已携带部件语义）
        → cosine_sim(query_feat, part_proto) → corr_map [方向二]
        → SAM decoder(pix_feat_with_mem, mask_inputs=corr_map)
        → 部件 mask
```

